@page "/logout"
@inject IJSRuntime JSRuntime;
@inject AccountService service;
@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            JSRuntime.InvokeVoidAsync("removeToken");
        }
    }
}